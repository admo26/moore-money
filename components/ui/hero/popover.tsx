"use client";

/** HeroUI v3 popover, re-exported under the app's own import path — same rationale as ./card.tsx. */
import {
  PopoverRoot as Popover,
  PopoverTrigger,
  PopoverContent,
  PopoverDialog,
  PopoverArrow,
  PopoverHeading,
} from "@heroui/react/popover";

export { Popover, PopoverTrigger, PopoverContent, PopoverDialog, PopoverArrow, PopoverHeading };
export type { PopoverProps } from "@heroui/react/popover";
