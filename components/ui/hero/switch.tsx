"use client";

/** HeroUI v3 switch, re-exported under the app's own import path — same rationale as ./select.tsx. */
import { Switch } from "@heroui/react/switch";

export { Switch };
export type { SwitchProps } from "@heroui/react/switch";
