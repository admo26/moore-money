"use client";

/** HeroUI v3 avatar, re-exported under the app's own import path — same rationale as ./card.tsx. */
import { AvatarRoot as Avatar, AvatarImage, AvatarFallback } from "@heroui/react/avatar";

export { Avatar, AvatarImage, AvatarFallback };
