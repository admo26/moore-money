"use client";

/** HeroUI v3 input, re-exported under the app's own import path — same rationale as ./card.tsx. */
import { Input as InputRoot } from "@heroui/react/input";

export { InputRoot as Input };
export type { InputProps } from "@heroui/react/input";
