"use client";

/** HeroUI v3 separator, re-exported under the app's own import path — same rationale as ./card.tsx. */
import { Separator as SeparatorRoot } from "@heroui/react/separator";

export { SeparatorRoot as Separator };
export type { SeparatorProps } from "@heroui/react/separator";
